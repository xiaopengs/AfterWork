{
  "framework": "nextjs",
  "buildCommand": "pnpm prisma generate && pnpm run build",
  "devCommand": "pnpm run dev",
  "installCommand": "pnpm install",
  "regions": ["sin1"],
  "env": {
    "NODE_ENV": "production"
  }
}
